doctype html
html
  head
    title isomorphic
    link(href='/styles.css', rel='stylesheet', type='text/css')
  body
    #isomorphic!= body
    script(src='/scripts.js')
    script.
      try {
        window.isomorphize(!{componentPath}, !{data});
      } catch (e) {
        try {
          window.console.error('cannot isomorphize!')
        } catch (e) {}
      }
